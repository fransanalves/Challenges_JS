// Write a function that takes a string (a) as argument
// Remove the first 3 characters of a
// Return the result
function removeFirst3Characters(str) {
    return str.slice(3);
}
module.exports = removeFirst3Characters;